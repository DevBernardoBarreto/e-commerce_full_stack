import { PlaceholderState } from "@/components/ui/PlaceholderState";

export const metadata = { title: "Carrinho" };

export default function CartPage() {
  return (
    <PlaceholderState
      title="Carrinho"
      description="Itens, quantidades e totais virão do servidor. Não há estado local de carrinho nesta etapa."
    />
  );
}
