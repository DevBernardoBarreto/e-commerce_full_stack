import { PlaceholderState } from "@/components/ui/PlaceholderState";

export const metadata = { title: "Pedidos" };

export default function OrdersPage() {
  return (
    <PlaceholderState
      title="Meus pedidos"
      description="O histórico autenticado aparece depois da API de orders."
    />
  );
}
